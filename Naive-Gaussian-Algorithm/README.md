# Naive Gaussian Algorithm

This project implements the Gaussian Naive Bayes algorithm using the Iris dataset.
It demonstrates data loading, model training, prediction, and evaluation using accuracy metrics.
